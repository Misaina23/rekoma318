{
  "name": "rekoma",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm start"
  }
}
