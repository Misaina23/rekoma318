{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "test -d frontend && cd frontend; npm install",
  "buildCommand": "test -d frontend && cd frontend; npm run build"
}
